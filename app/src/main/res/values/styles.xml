<resources>

    <!-- Base application theme. -->
    <style name="Theme.AppCompat" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- Menu item padding top-->
    <style name="ItemPaddingTop" parent="Widget.AppCompat.ActionButton">
        <item name="android:minWidth">0dip</item>
        <item name="android:paddingTop">5dp</item>
    </style>

        <!--    Slider-->
    <style name="SliderBarTheme">
        <item name="android:progressBackgroundTint">@color/primary</item>
        <item name="android:progressTint">@color/primary</item>
        <item name="android:colorControlActivated">@color/secondary</item>
    </style>


</resources>
